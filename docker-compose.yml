version: '3'

services:
  postgres:
    image: postgres:11
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=gympoint
      - POSTGRES_PASSWORD=docker
    volumes:
      - ./postgresdata:/var/lib/postgresql/data
